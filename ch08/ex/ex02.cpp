// What are the similarities and differences
// between these three overloaded functions:
// int DoSomething(int num1, int num2);
// int DoSomething(int& num1, int& num2);
// int DoSomething(int* pNum1, int* pNum2);

// int DoSomething(int num1, int num2);
// Takes 2 ints as parameters and returns int.

// int DoSomething(int& num1, int& num2);
// Takes 2 references to ints as parameters and returns int.

// int DoSomething(int* pNum1, int* pNum2);
// Takes 2 pointers to ints as parameters and returns int.
